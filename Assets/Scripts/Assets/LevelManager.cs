using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelManager : MonoBehaviour {

	/*
	public static Dictionary <int, Level> levels;

	// Use this for initialization
	void Awake() {
		levels = new Dictionary < int, Level> ();
		Level newLevel;
		newLevel = new Level ();
		newLevel.levelIndex = 1;
		newLevel.maxFuel = 1000;
		newLevel.startingPosition = Vector3.zero;
		newLevel.fuel2star = 800;
		newLevel.fuel3star = 500;
		newLevel.time2star = 25;
		newLevel.time3star = 80;

		levels [1] = newLevel;

		newLevel = new Level ();
		newLevel.levelIndex = 1;
		newLevel.maxFuel = 1200;
		newLevel.startingPosition = Vector3.zero;
		newLevel.fuel2star = 920;
		newLevel.fuel3star = 600;
		newLevel.time2star = 35;
		newLevel.time3star = 100;

		levels [2] = newLevel;
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
